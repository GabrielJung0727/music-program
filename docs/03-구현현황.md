# Mono 구현 현황

[`01-기획명세서.md`](01-기획명세서.md)의 기능 표를 기준으로, 지금 코드가 어디까지 왔는지와 남은 한계를 적는다.
✅ 동작 · 🟡 동작하되 범위 제한 · ⬜ 미구현

## 1. 핵심 기술

| 기능 | 상태 | 구현 위치 · 비고 |
| --- | --- | --- |
| 초저지연 음원 공유 | ✅ | `FanOutService`(lookahead 320ms) + Output 지터 버퍼. 같은 호스트 Exclusive 실측 offset −0.16ms / jitter 0.33ms |
| MATP | ✅ | `MatpFrame`(44B 헤더 + AES-GCM), 컨트롤/데이터 플레인 분리, `epoch`로 리싱크 |
| 마스터 클럭 · 멀티 엔드포인트 | ✅ | `ClockSync`(offset·rtt·지터 추정, 적응형 버퍼 5–80ms), Output → Core `clock_report` |
| Bit-perfect 경로 | ✅ | DSP Off·Audiophile에서 입력 그대로 전달. 하드웨어 볼륨 우선, 디지털 감쇠는 정책이 막음 |
| 고해상도 DSP | ✅ | `DspPipeline`: 업샘플·틸트·크로스피드·Easy EQ·Device EQ·Speaker·headroom. **룸 IR WAV/ZIP 로딩·컨볼루션** |
| 메타데이터 그래프 | ✅ | `CatalogStore.Graph` — 아티스트·앨범·트랙·크레딧·연관/동시대 아티스트, 전부 로컬 쿼리 |
| 고성능 로컬 카탈로그 | ✅ | SQLite + 인덱스, 메모리 그래프 캐시 (`catalog.db`) |
| 스마트 선택형 오토플레이 | ✅ | `RoomManager.ProposeAutoplayCandidates`(큐 마지막 곡 종료 40초 전 후보 3곡) + `ChooseAutoplay` + `AdvanceFinished`가 선택/1번 후보로 자동 전환 |
| 다국어 아티스트 검색 | 🟡 | `Artist.AlternateNames` + `data/aliases.json` 시드(한·영·일). **MusicBrainz 원격 API는 미연동** |
| 위키백과 아티스트 이력 | ✅ | `WikipediaService` — ko→en 폴백, 정식명+별칭을 표제어 후보로 순차 시도, 메모리+파일(`data/wiki`) 캐시 30일. `/api/wiki/{artistId}` |

## 2. 유틸리티

| 기능 | 상태 | 구현 위치 · 비고 |
| --- | --- | --- |
| 라이브러리 통합 | 🟡 | `LibraryScanner` — FLAC/WAV/AIFF/ALAC/M4A/MP3/AAC/DSF/DFF 태그·커버·LRC. 재생은 MF 실디코드(전량 RAM). 스트리밍은 **OAuth 스텁 + 데모 카탈로그** (파트너 SDK 대기) |
| 로컬/스트리밍 병합 표시 | ✅ | 제목 매칭으로 `MergedLocalAndStreaming` 표시, 배지에 `Local+Stream` |
| 출력 엔드포인트 관리 | ✅ | `EndpointRegistry`(오프라인 기기 기억) + 능력 광고(포맷·지연·볼륨·배타 모드) |
| 멀티 디바이스 존 (싱글 플레이) | ✅ | `ZoneRegistry` + `CommandProcessor.MoveDeviceIntoZone` — Sync는 공유 개인 방, Independent는 기기별 개인 방. 연결 유지한 채 방만 재배정(`LeaveCurrentRoomAsOutput`) |
| 라운지 생성/참여 | ✅ | 4개 모드, 초대 코드 발급/연장/폐기, 최대 인원, 능력 협상 실패 시 사유 표시 |
| 공동 큐 빌더 | ✅ | 추가·삭제·순서 변경·비우기·바로 이동, 중복/권한/스트리밍 미구독 검증, 요청·승인·거절 |
| 동기화 가사 | ✅ | `LyricsParser` + Control에서 `media_time` 따라 하이라이트·자동 스크롤 |
| 세션 아카이브 | ✅ | 종료 시 동의 → SQLite 영속화. 미동의 시 휘발. 익명화·보존기간 정책 반영 |
| 세션 → 플레이리스트 | ✅ | 핀/♥ 기준 하이라이트 플레이리스트 자동 생성, M3U 내보내기(`/api/m3u/{id}`) |
| 청음 히스토리 | ✅ | `HistoryStore` 개인 타임라인, 완청 표시, 히스토리에서 큐로 다시 싣기 |

## 3. 편의 · 인터랙션

| 기능 | 상태 | 구현 위치 · 비고 |
| --- | --- | --- |
| 첫 실행 온보딩 튜토리얼 | ✅ | Roon 튜토리얼(언어·서버·로그인·폴더·스트리밍·기기) 구조를 Mono 아키텍처에 맞게 재구성한 6단계 마법사 — 이름 설정 → 라이브러리 스캔 → 출력 장치/존 설정 → 스트리밍 연동 → 완료. 헤더의 "가이드" 버튼으로 언제든 재실행 |
| 크로스 디바이스 Control | ✅ | **Avalonia `Mono.Control.exe`(WinExe).** TCP `:7700`. Core/Output 자동 기동. 레거시 `wwwroot` SPA **삭제**. `Mono.Cli`는 개발용 |
| 앨범 아트 퍼포먼스 | 🟡 | Core `/api/art/{id}` + Control `ArtCache`로 그리드·하단바·Now Playing 로드. **60fps 실측은 아직** |
| DSP 프리셋 · 룸 잠금 | ✅ | Off/Headphones/Speakers/RoomIr/Crossfeed/Parametric, Easy EQ·Device EQ UI, `dsp_lock`, Audiophile 강제 Off |
| 포맷·품질 배지 | ✅ | 룸 배지 + 엔드포인트별 배지(SRC 여부·스트리밍 등급) |
| 타임스탬프 코멘트 | ✅ | 핀 생성/삭제, 시크바 마커, 클릭 시 해당 구간 이동(정책 준수) |
| 라이너 노트 공동 열람 | 🟡 | 라이너·크레딧·수록곡 패널 + `follow_host` 플래그. **호스트 스크롤 위치 추종은 페이지 단위까지** |
| 뮤지션 관계도 | ✅ | 그래프 패널에서 탐색, "따라가기"로 큐/요청 투입 |
| 최다 반응 구간 (리액션 히트맵) | ✅ | `RoomManager.React`(❤️🎉👏🔥 화이트리스트, 곡당 유저당 3회 제한) + `ReactionHeatmap`(10초 버킷) + `HistoryStore.TrackHeatmap`(세션 전체 합산, `/api/reactions/{trackId}`) |
| 라운지 채팅/반응 | ✅ | 저대역폭 텍스트·이모지, Audiophile 기본 접힘 |
| 공동 DJ 핸드오프 | ✅ | `set_role`로 DJ 위임, `transfer_host`는 Core가 원자적으로 처리 |

## 4. 관리자 기능

| 기능 | 상태 | 구현 위치 · 비고 |
| --- | --- | --- |
| 룸 운영 제어 | ✅ | 킥(재입장 차단), 초대 만료/폐기, 큐 잠금, 시킹·코멘트 허용, 최대 인원, 자동 다음곡 |
| 품질 정책 엔진 | ✅ | Bit-perfect 전용 / 최저 공통 포맷 / 불가 시 참관. 참관자는 데이터 플레인 제외 |
| 세션 동의·보존 정책 | ✅ | 기본 저장 동의, 보존 기간(`RetentionService`가 주기 집행), 익명화 |
| Core 서비스 운영 | 🟡 | 백그라운드 서비스·주기 스캔·로그·엔드포인트 상태·페어링 토큰. **클라우드 동기화는 opt-in 플래그만** |

## 5. 비기능 요구사항

| 항목 | 상태 | 비고 |
| --- | --- | --- |
| 음질(소셜이 오디오를 흔들지 않을 것) | ✅ | 소셜은 컨트롤 세션, 오디오는 MATP. Output도 수신/렌더 루프 분리 |
| 동기화 < 5ms | 🟡 | 같은 호스트 Exclusive에서 sub-ms 락 확인. **실제 LAN 다중 엔드포인트 실측은 남음** |
| 확장 (Core 1 : Control N : Output M) | ✅ | Control 없이 Output+Core만으로도 재생 지속(자동 다음곡 포함) |
| 보안 | ✅ | 팬아웃 페이로드 AES-GCM, 원격 Control 페어링 토큰, 파일은 피어에 재배포하지 않음 |
| 라이선스 분기 | ✅ | 스트리밍은 자동으로 Clock-sync, 팬아웃은 로컬 파일에 한정 |

## 6. 남은 한계 (다음 작업 후보)

1. **디코더 메모리**: FLAC/ALAC/MP3는 Media Foundation으로 실디코드되지만 **파일 전체를 RAM에 적재**한다. 스트리밍 슬라이스/디스크 캐시가 다음 조각.
2. **DSD 렌더**: Output에 DoP(WASAPI) 경로가 있다. ASIO 네이티브 DSD·DSF 채널 인터리브 정합은 실기기 검증 필요.
3. **스트리밍 SDK**: Tidal/Qobuz는 OAuth URL 스텁 + 데모 토큰·카탈로그. **파트너 Client ID / 실 스트림 URL** 연동 대기.
4. **Avalonia UX**: Home 레일·Genres 타일·Now Playing 몰입(블러/가사/아티스트/크레딧)·Easy EQ·시그널 패스·폴더 피커·트레이 종료 정책은 반영됨. Roon 전 기능 패리티는 범위 밖.
5. **LAN 다중 엔드포인트 검증**: `sync_probe`로 진단 가능. **서로 다른 물리 기기 2대 이상에서 5ms 목표 실측**은 남음.
6. **다국어 검색 확장**: `aliases.json` 시드 확장됨. MusicBrainz API 자동 수집은 미연동.
7. **Zone Independent 모드 실측**: 로직 구현됨. 물리 기기 2대+ “각자 다른 곡” 시나리오 검증 남음.
