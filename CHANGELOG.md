# 변경 내역

릴리스 노트는 여기 한 곳에만 둔다. `publish.ps1` 이 `-Version` 에 해당하는 절을 그대로 꺼내
GitHub 릴리스 본문으로 쓴다 — 스크립트에 문구를 박아 두면 다음 릴리스에서 낡은 채로 나간다.

## 0.5.9

0.5.7 재검증 취합표(4건)를 반영했습니다.

### 이전 곡 버튼이 다음 곡으로 넘어가던 문제

「이전」을 눌러도 다음 곡으로 갔습니다. 상시 발생했습니다.

웹은 건너뛰기를 `delta` 로 보내는데 Core 는 `index` 만 읽고 있었습니다. 웹이 보낸 -1 이
통째로 무시되고 기본값 1 이 쓰였습니다 — 「이전」과 「다음」이 같은 명령이 된 셈입니다.
이제 둘 다 받습니다.

- 곡을 듣던 도중에 누른 「이전」은 그 곡을 처음부터 다시 틉니다. 실수로 한 번 눌렀다고
  듣던 곡을 잃지 않습니다.
- 곡의 맨 앞(3초 이내)에서 누르면 앞 곡으로 넘어갑니다.

### ASIO 에서 WASAPI 로 돌아올 때 장치가 열리지 않던 문제

ASIO 로 듣다가 WASAPI(공유/배타)로 바꾸면 간헐적으로 소리가 나지 않았습니다. 특히
기본 장치로 바꿀 때 자주 나타났습니다.

장치를 놓아줄 시간을 줄지 말지를 "워커가 지금 돌고 있는가"로 판단하고 있었습니다.
워커가 이미 죽었거나 먼저 끊긴 뒤에는 그 조건이 거짓이 되어 곧바로 장치를 열었는데,
드라이버 핸들은 프로세스보다 오래 남습니다. 이제 "직전에 무엇으로 열었는가"로 판단하고,
워커의 생사와 무관하게 기다립니다.

- ASIO 가 끼지 않은 전환(배타 ↔ 공유, 장치 변경)도 짧게 기다립니다. 엔드포인트가 다 풀리기
  전에 다시 열면 같은 장치를 두 번 잡는 모양이 됩니다.
- 장치가 잠겨 있을 때 다시 열어 보는 시간을 2.4초에서 4.8초로 늘렸습니다. 끈질긴 장치는
  2초를 넘기기도 해서, 예전 창에서는 기다림이 모자라 그대로 실패했습니다.

### 컴필레이션 앨범의 아티스트

참여 음악가가 아니라 앨범 음악가가 트랙의 아티스트로 올라오던 문제입니다. 0.5.7 에서
스캐너를 고쳤지만, 주기 스캔이 기본 폴더 하나만 훑고 있어서 직접 추가한 폴더의 곡은
아무도 스캔을 누르지 않는 한 옛 메타데이터 그대로였습니다.

이제 설정된 폴더를 전부 훑습니다. 업데이트한 뒤 따로 하실 일은 없습니다 — 기동 직후와
30분마다 도는 스캔이 제자리를 찾아 줍니다.

### 탐색

곡 위치를 옮겼을 때 재생바가 따라오지 않던 문제는 0.5.8 에서 고쳤습니다. 이번 판에서도
같이 확인했습니다.

## 0.5.8

0.5.7 회귀 테스트에서 나온 진행 바 문제를 고쳤습니다.

### 탐색한 뒤 진행 바가 멈춰 있던 문제

노래는 옮긴 자리에서 이어지는데 진행 바만 손 뗀 자리에 붙어 움직이지 않았습니다.
원인은 두 가지였습니다.

바의 로컬 값을 진행 바 위에서 손을 뗄 때만 비웠습니다. 바는 높이가 3px 이라 조금만
벗어나도 바깥에서 손을 떼게 되는데, 그 한 번을 놓치면 로컬 값이 계속 남아 재생 시계를
가렸습니다. 이제 로컬 값은 아직 확정되지 않은 요청일 뿐입니다 — 창 어디에서 손을 떼도
확정되고, 확정되지 않았더라도 조작이 멎으면 시계로 돌아갑니다.

또 하나는 0.5.7 이 만든 문제입니다. 길이를 모르는 트랙에 3분이라는 추정치를 씌워
화면에 내보내고 있었습니다. 6분짜리 곡이라면 3분 지점에서 바가 끝에 닿아 멈춥니다.

- 이제 실제로 아는 길이만 내보냅니다. 모르면 총 시간이 `--:--` 로 뜨고 바는 눈금을
  잡지 않습니다.
- 길이는 재생을 시작할 때 디코더에게 물어 채우고 라이브러리에 남깁니다. 다음부터는
  처음부터 제대로 보입니다.
- 곡의 맨 앞에서 바가 3분의 1쯤 차 있던 것도 고쳤습니다.

### 라운지 전체 목록

Live Lounges 첫 화면은 스포트라이트와 카테고리로 고른 큐레이션이라 방이 늘어나면 다
담지 못합니다. 필터 줄 끝의 **All Rooms** 로 전체 목록을 엽니다. 카드와 리스트 두 가지로
보고, 제목·호스트·아티스트로 검색하고, 음질(DSD·Hi-Res·Vinyl)로 좁힐 수 있습니다.

### 그 밖에

- 커버가 비어 있을 때 브라우저가 페이지를 통째로 다시 받게 만들던 자리를 정리했습니다.
  0.5.7 의 기본 커버는 불러오기에 실패한 경우만 잡았고, 아예 비어 있는 경우는 남아
  있었습니다.

## 0.5.7

0.5.6 QA 취합표(버그 7건)를 반영했습니다.

### 앨범 가운데 곡을 눌러도 앨범 순서로 재생합니다

앨범 화면에서 가운데 곡을 고르면 그 한 곡만 큐 끝에 붙었습니다. 앨범은 차례대로 들리는데
큐에는 한 곡만 있으니 순서가 뒤엉킨 것처럼 — 셔플이 저절로 켜진 것처럼 — 보였습니다.

원인은 명령을 네 번에 나눠 보낸 것이었습니다. 큐 비우기 · 곡 넣기 · 그 칸으로 이동 · 재생을
따로 보내면 그 사이사이의 룸 상태가 방송되므로, 큐가 한 곡짜리인 순간이 화면에 그대로
남았습니다. 이제 한 번의 명령으로 앨범 전체를 큐에 걸고 고른 곡을 시작점으로 잡습니다.

- 앨범 전체 재생도 같은 경로를 씁니다. 큐에 앨범의 곡 목록이 순서대로 들어옵니다.
- 같은 앨범을 다시 틀어도 큐가 두 배로 늘지 않습니다.

### 셔플과 반복이 실제로 동작하고, 켜졌는지 보입니다

플레이어의 셔플·반복 버튼은 그림만 있었고 누르면 아무 일도 일어나지 않았습니다. 켜졌는지
꺼졌는지 알 방법도 없었습니다.

- 셔플은 큐의 순서를 흐트러뜨리지 않고 "다음에 무엇을 트는가"만 바꿉니다. 큐 자체를 섞으면
  셔플을 끈 뒤에도 앨범 순서가 돌아오지 않습니다.
- 반복은 끔 → 전체 반복 → 한 곡 반복을 돕니다. 한 곡 반복은 아이콘에 1 이 붙습니다.
- 켜진 버튼은 색이 바뀌고 아래에 점이 남습니다. 라운지에서는 파랑, 혼자 들을 때는 보라입니다.
- 게스트에게도 호스트가 정한 상태가 그대로 보입니다. 다만 잠겨 있고 명도가 낮습니다.

### 곡 중간으로 탐색했을 때 처음부터 다시 재생되던 문제

길이를 모르는 트랙에 추정치 3분을 씌우고 있었습니다. 6분짜리 곡의 3분 뒤로 탐색하면 그
자리에서 "다 들었다"가 되고, 오토플레이가 같은 곡을 큐에 다시 넣었습니다 — 탐색했더니 곡이
처음부터 다시 시작하는 것으로 보인 경로입니다.

- 길이는 스캔할 때 반드시 채웁니다. 태그에 없으면 디코더에게 직접 묻습니다.
- 그래도 길이를 모르는 트랙은 추정치로 끝내지 않습니다. 추정치 뒤로도 탐색됩니다.
- 아는 길이가 있으면 그 안으로만 탐색합니다.
- 탐색 직후 Core 가 밀어 준 오디오를 Output 이 버리던 문제를 함께 고쳤습니다. 새 세대의
  오디오가 그 세대를 알리는 룸 상태보다 먼저 도착하면 전부 버려져, 탐색 지점의 앞부분이
  통째로 사라졌습니다. 이제 큐에 두었다가 타임라인이 따라잡으면 그대로 씁니다.

### 커버가 없는 곡에서 깨진 이미지가 뜨지 않습니다

엘범이 없는 곡을 틀고 라운지에서 호스트를 열면 커버 자리가 깨진 이미지로 보이고, 그 자리가
호스트와 멤버 목록까지 밀어냈습니다.

기본 커버 이미지를 넣었습니다. 커버가 비었을 때도, 경로는 있는데 파일이 사라졌을 때도 같은
이미지가 뜹니다. 커버를 그리는 자리가 수십 군데라 한 곳씩 막는 대신 앱 전체에 한 번 겁니다.

### 컴필레이션 앨범의 아티스트

컴필레이션 음원을 스캔하면 참여 음악가가 아니라 앨범 음악가가 트랙의 아티스트로 올라왔습니다.
이제 트랙의 아티스트는 참여 음악가가 먼저입니다. 참여 음악가가 없을 때만 앨범 음악가를 씁니다.

앨범을 묶는 기준은 그대로 앨범 음악가입니다 — 참여 음악가로 묶으면 컴필레이션 한 장이
참여자 수만큼 쪼개집니다.

### Home · Repertoire 에서 다이나믹 레인지와 비트퍼펙트를 뺐습니다

Repertoire 카드는 폭이 좁아 포맷 배지와 DR 배지를 나란히 두면 둘 다 잘렸습니다. 잘린 숫자는
없는 것만 못합니다. DR 배지와 DR 정렬을 카드에서 뺐습니다.

비트퍼펙트는 유저가 설정에서 직접 고르는 것이라 라이브러리 통계로 단정할 값이 아닙니다.
Home 지표에서 뺐고, 그 자리에 앨범 수와 이번 주 재생 횟수를 둡니다.

## 0.5.2

### 재생 중 잡음을 고쳤습니다

배타(비트퍼펙트) 모드에서 24bit 로 재생하면 소리 전체에 잡음이 섞이던 문제를 고쳤습니다.

원인은 장치에 넘기던 요청 규격이었습니다. 24bit 를 `WAVEFORMATEX` 로 요청하면 3바이트로
담으라는 뜻인지 4바이트 컨테이너의 상위 24비트라는 뜻인지가 정해지지 않습니다. 드라이버는
그 모호한 요청에도 "지원한다"고 답한 뒤 배타 모드에서 프레임을 4바이트씩 읽어 갔습니다.
큐가 실시간의 1.35배(=32/24)로 빠지면서 첫 샘플부터 모든 경계가 밀렸습니다.

이제 24bit 는 컨테이너를 명시한 `WAVEFORMATEXTENSIBLE`(32비트 컨테이너 / 24비트 유효)로
엽니다. 같은 장치에서 큐 소비 속도가 1.00배로 돌아왔습니다.

- 32bit WAV(float·정수)에서 뎁스만 16/24 로 접고 바이트는 그대로 넘기던 문제를 고쳤습니다.
  8bit·64bit float 파일도 같은 경로로 바르게 변환합니다.
- WAV 를 이어 읽을 때 청크마다 밀리초를 바이트 위치로 다시 계산하느라 반올림 경계에서
  한 프레임씩 빠지던 문제를 고쳤습니다. 초당 수십 번의 딸깍 소리로 들리던 증상입니다.
- 장치가 지원하지 않는 32bit 를 지원한다고 보고하던 문제를 고쳤습니다.

### 배타 모드가 거절되면 공유 모드로 이어 재생합니다

장치가 배타 점유를 받지 않거나 다른 프로그램이 점유 중이면 재생이 멈추는 대신 WASAPI 공유
모드로 이어서 재생합니다. 다만 조용히 내려가지는 않습니다 — 비트퍼펙트 표시가 내려가고
사유가 설정 화면에 그대로 뜹니다. 강등 없이 멈추던 예전 동작이 필요하면 출력 백엔드를
`exclusive-strict` 로 두십시오.

### 고른 출력 장치로 실제로 재생합니다

마법사에서 장치를 골라도 플레이어와 설정의 "출력 연결"이 장치를 넘기지 않아, 재생은 계속
OS 기본 장치로 나가던 문제를 고쳤습니다. 화면에는 고른 장치 이름이 떠 있어서 알아채기
어려운 증상이었습니다.

- 설정 → Audio Engine 에서 이 PC 의 출력 장치를 직접 고르고 바꿀 수 있습니다. 고르면
  즉시 그 장치로 전환됩니다. 장치 목록을 다시 훑는 Rescan 도 있습니다.
- Exclusive Mode 토글이 저장되고 실제로 적용됩니다. 예전에는 화면에만 있었고, 누르면
  마법사에서 고른 ASIO 설정을 덮어쓰기까지 했습니다.
- 워커가 이미 돌고 있을 때 장치나 백엔드를 바꿔도 무시하던 문제를 고쳤습니다.
- 재생할 룸이 없을 때 출력을 연결하면 워커는 뜨지만 어떤 룸에도 붙지 않습니다. 그런데도
  "연결했습니다" 라고 떠서, 소리가 안 나는 이유를 알 수 없었습니다. 이제 장치가 실제로
  붙었는지 확인하고 말하며, 재생을 시작하면 그 룸으로 워커를 다시 붙입니다.
- 플레이어 하단 출력 이름에 `[object Object]` 가 붙던 문제를 고쳤습니다.

### 곡을 튼다고 호스트가 되지 않습니다

재생을 시작하면 공개 라운지가 강제로 만들어지던 문제를 고쳤습니다. 혼자 듣기는 이제 라운지
목록에 뜨지 않습니다. 호스트는 "Host Lounge" 로 직접 고르는 것이 됩니다.

원인은 프로토콜 값이 어긋난 것이었습니다. 웹은 혼자 듣기를 2 로 보냈는데 Core 의 2 는
HostQueue — 진짜 라운지였습니다. Core 에 혼자 듣기 모드를 따로 두고 목록에서 뺐습니다.
같은 어긋남 때문에 자기 재생인데도 탐색이 막혀 있던 것도 함께 풀렸습니다.

호스트를 시작하면 듣던 방이 그 자리에서 라운지가 됩니다 — 큐와 재생 위치가 그대로라
틀어 둔 곡이 끊기지 않고, 방이 두 개로 갈라지지도 않습니다.

### Mono 는 한 번에 하나만 뜹니다

같은 PC 에서 Mono 를 두 벌 이상 띄울 수 있었습니다. 두 벌이 뜨면 나중 것이 앞선 인스턴스의
Core 에 붙어, 한 PC 가 두 사람처럼 보입니다 — 같은 이름의 라운지가 둘씩 생기고, 출력 워커
두 개가 같은 DAC 를 배타로 열려고 다툽니다. 화면에는 아무 표시도 나지 않았습니다.
이제 두 번째로 실행하면 이미 떠 있는 창을 앞으로 부르고 조용히 물러납니다.

### 라운지는 한 사람당 하나입니다

한 사람이 라운지를 여러 개 열 수 있었습니다. 대부분은 곧 비는 방이고, 듣는 쪽에서는 어느
것이 진짜인지 알 수 없습니다. 이미 열어 둔 라운지가 있으면 사유를 알려 드립니다 —
살아 있는 방을 말없이 닫지는 않습니다. 혼자 듣기는 라운지가 아니므로 제한에 걸리지 않습니다.

### 빈 라운지가 쌓이지 않습니다

만들어진 방을 지우는 경로가 아예 없어서, 한 번 생긴 라운지는 Core 가 꺼질 때까지 목록에
남았습니다. 창을 닫거나 새로고침해도 나간 것으로 처리되지 않아 방은 영영 "비지 않은"
상태였습니다.

- 연결이 끊기면 방에서도 나갑니다.
- 아무도 없는 방은 잠시 뒤 사라집니다. 새로고침으로 잠깐 비는 동안 듣던 큐를 잃지 않도록
  유예를 둡니다.
- "End Session" 이 이제 라운지를 실제로 닫습니다. 예전에는 호스트만 빠져나가고 방은
  목록에 그대로 남았습니다.

### 첫 실행 마법사

출력 아키텍처 기본값이 ASIO 라서, ASIO 드라이버가 없는 PC 에서는 장치 목록이 빈 채로 떴습니다.
그대로 넘어가면 저장되는 오디오 설정이 없어 이후 재생이 계속 OS 기본 장치로 나갔습니다.
이제 실제로 장치가 있는 아키텍처로 맞추고 시스템 기본 출력을 미리 골라 둡니다. 아키텍처를
직접 고르셨다면 그 선택은 그대로 둡니다.
