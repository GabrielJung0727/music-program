<Project>
  <PropertyGroup>
    <!-- 저장소 기본값. publish.ps1 이 -p:Version 으로 덮어쓴다.
         InformationalVersion 을 따로 박으면 그쪽이 이겨서 옛 숫자가 남는다. -->
    <Version>0.1.0</Version>
  </PropertyGroup>
</Project>
